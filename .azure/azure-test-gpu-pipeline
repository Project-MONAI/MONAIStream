trigger: none
  
stages:
  - stage: 'Test'
    jobs:
    - job: RunTests
      pool: "Monai VMSS"
      strategy:
        matrix:
          Python3.6:
            python.version: '3.8'
        maxParallel: 2
      steps:
      # - task: UsePythonVersion@0
      #   inputs:
      #     versionSpec: $(python.version)
      - script: |
          docker build .
          